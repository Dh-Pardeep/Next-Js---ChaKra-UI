/* theme.ts */
import { extendTheme } from "@chakra-ui/react";

export const theme = extendTheme({
  fonts: {
    fontroboto: "var(--font-roboto)",
    fontmerriweather: "var(--font-merriweather)",
  },
});
